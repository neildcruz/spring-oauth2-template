server:
  port: 8888
  
logging:
  level:
    com.netflix.discovery: 'OFF'
    
eureka:
  client:
    service-url:
      defaultZone: ${EUREKA_SERVER_URL:http://localhost:8761/eureka}
    healthcheck:
      enabled: true